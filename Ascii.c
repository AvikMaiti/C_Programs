//  Ascii Value of the Character

// Header file
#include <stdio.h>

// Main function
int main(void)
{
    char in;
    unsigned long long int fact;
    printf("Enter a character: ");
    scanf("%c", &in);
    printf("The ascii value of %c is %d.", in, in);
}